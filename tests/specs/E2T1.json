[
  {
    "name": "/daterange shows quick date-range buttons",
    "steps": [
      {
        "send": { "text": "/daterange" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Select a date range:" } }
        ]
      }
    ]
  },
  {
    "name": "Last month button stores selection and confirms",
    "steps": [
      {
        "send": { "text": "/daterange" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Select a date range:" } }
        ]
      },
      {
        "send": { "callback": "dr:last_month" },
        "expect": [
          { "method": "answerCallbackQuery" },
          { "method": "sendMessage" }
        ]
      }
    ]
  },
  {
    "name": "Last year button stores selection and confirms",
    "steps": [
      {
        "send": { "text": "/daterange" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Select a date range:" } }
        ]
      },
      {
        "send": { "callback": "dr:last_year" },
        "expect": [
          { "method": "answerCallbackQuery" },
          { "method": "sendMessage" }
        ]
      }
    ]
  },
  {
    "name": "Custom range button prompts for date input",
    "steps": [
      {
        "send": { "text": "/daterange" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Select a date range:" } }
        ]
      },
      {
        "send": { "callback": "dr:custom" },
        "expect": [
          { "method": "answerCallbackQuery" },
          { "method": "sendMessage", "payload": { "text": "Enter the start date (YYYY-MM-DD):" } }
        ]
      }
    ]
  },
  {
    "name": "Valid custom date input is stored",
    "steps": [
      {
        "send": { "text": "/daterange" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Select a date range:" } }
        ]
      },
      {
        "send": { "callback": "dr:custom" },
        "expect": [
          { "method": "answerCallbackQuery" },
          { "method": "sendMessage", "payload": { "text": "Enter the start date (YYYY-MM-DD):" } }
        ]
      },
      {
        "send": { "text": "2024-01-01" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Enter the end date (YYYY-MM-DD):" } }
        ]
      },
      {
        "send": { "text": "2024-06-30" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Date range set: 2024-01-01 to 2024-06-30" } }
        ]
      }
    ]
  },
  {
    "name": "Invalid custom date format shows error",
    "steps": [
      {
        "send": { "text": "/daterange" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Select a date range:" } }
        ]
      },
      {
        "send": { "callback": "dr:custom" },
        "expect": [
          { "method": "answerCallbackQuery" },
          { "method": "sendMessage", "payload": { "text": "Enter the start date (YYYY-MM-DD):" } }
        ]
      },
      {
        "send": { "text": "not-a-date" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Invalid date. Enter the start date as YYYY-MM-DD (e.g. 2024-01-15):" } }
        ]
      }
    ]
  }
]